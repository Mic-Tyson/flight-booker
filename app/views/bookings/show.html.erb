<h2>Bookings</h2>
<% @bookings.each do |booking| %>
  <%= booking.flight.departure.airport_code %> to <%= booking.flight.arrival.airport_code %> on <%= booking.flight.start %>
  <% booking.passengers.each do |passenger| %>
   <%= passenger.name %>
  <% end %>
<% end %>